services:
  - type: web
    name: penline-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /health
    envVars:
      - key: FRONTEND_URL
        sync: false   # set this to your Netlify URL after first deploy
